package com.testenv.bl;

import com.testenv.models.Map;
import com.testenv.models.UserAction;

public class Collision {
    public static boolean validate(Map map, UserAction userAction) {
        boolean result = true;
        return result;
    }
}
